name: iceSphereCamera
description: 720度球面全景看房应用

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  dio: ^5.4.0
  provider: ^6.1.1
  shared_preferences: ^2.2.2
  camera: ^0.10.5
  sensors_plus: ^4.0.2
  permission_handler: ^11.3.0
  cached_network_image: ^3.3.1
  panorama: ^0.4.1
  image_picker: ^1.0.7
  flutter_spinkit: ^5.2.0
  path_provider: ^2.1.2
  uuid: ^4.3.3
  share_plus: ^7.2.1
  ftpconnect: ^2.0.5
  archive: ^3.4.10

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
